int sub_10450E00()
{
  sub_1042C230(
    byte_106929B4,
    (int)"npc_create_aimed",
    (int)sub_100641C0,
    "Creates an NPC aimed away from the player of the given type where the player is looking (if the given NPC can actual"
    "ly stand at that location).  Note that this only works for npc classes that are already in the world.  You can not c"
    "reate an entity that doesn't have an instance in the level.\n"
    "\tArguments:\t{npc_class_name}",
    0x4000,
    0);
  return atexit(sub_1046EF00);
}
